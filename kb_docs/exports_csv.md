# Data export: CSV exports

CSV exports live under **Reports → Export**.

If columns are missing:
- confirm the export template
- check if fields are enabled in workspace settings

For very large exports:
- export smaller date ranges
- use the API export endpoint
