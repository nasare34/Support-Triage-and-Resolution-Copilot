# API: Tokens & Scopes

Create tokens in **Settings → Developer → API Tokens**.

Recommended scopes:
- Read-only for analytics/export jobs
- Webhook management only for integration admins

Security:
- store tokens in a secret manager
- rotate tokens every 90 days
