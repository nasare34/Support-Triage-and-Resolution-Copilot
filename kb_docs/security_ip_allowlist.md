# Security: IP allowlisting

Business/Enterprise can restrict access by IP ranges.

Steps:
- Settings → Security → IP Allowlist
- Add CIDR ranges

Notes:
- VPN egress IPs must be included
- If you lock yourself out, support can temporarily disable allowlisting
