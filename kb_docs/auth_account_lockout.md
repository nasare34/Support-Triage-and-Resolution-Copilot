# Authentication: Account lockout

Accounts lock after repeated failed attempts.

Admin recovery:
- Admin → Users → Unlock account

If the user cannot login after unlock:
- verify SSO requirements
- check if MFA is enforced
